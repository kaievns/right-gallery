<% if @picture.valid? %>
  <% flash.clear %>
  update_picture(<%= @picture.id %>, "<%= escape_javascript(render(@picture))%>");
<% else %>
  replace_form('edit_picture_<%= @picture.id%>', "<%= escape_javascript(render(@picture)) %>");
<% end -%>