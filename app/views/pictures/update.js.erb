<% if @picture.valid? %>
  <% flash.clear %>
  update_picture(<%= @picture.id %>, "<%= escape_javascript(render(@picture))%>");
<% else %>
  $('edit_picture_<%= @picture.id%>').replace("<%= escape_javascript(render(@picture)) %>");
  remotize_form($('edit_picture_<%= @picture.id %>'));
<% end -%>